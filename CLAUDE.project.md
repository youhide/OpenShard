# Project-specific additions — OpenShard

A modern Ultima Online server engine, compatible with the original 2D client and
ClassicUO. **Not a SphereServer clone** — the engine SphereServer would likely be
if it were designed today: compatible with the UO *protocol*, and with nothing
else about Sphere. Gameplay content lives in a second repository, the
**OpenShard Community Pack**.

## Docs — read before you touch code

| | |
|---|---|
| [`docs/style.md`](docs/style.md) | How code here reads. Read it before writing Rust in this repo. |
| [`docs/architecture.md`](docs/architecture.md) | The shape: layers, dependency rules, the crate map. |
| [`docs/findings.md`](docs/findings.md) | What the client actually does. Every entry cost a day — don't re-derive them. |
| [`docs/README.md`](docs/README.md) | 🚩 **What each area does today, and where each kind of document lives.** The roles (canon · design · research · evidence · reference · plan) and the domain map. Current status lives in a domain's README, never in this file. |
| [`plans/roadmap/PLAN.md`](plans/roadmap/PLAN.md) | The order of work across the engine — which area is next and what has to be true first. No status in it; a plan carries no progress. |
| [`docs/roadmap/README.md`](docs/roadmap/README.md) | What is left of the old roadmap. Every phase record has been claimed by its domain, so what stays is the index of where each went, two cross-domain backlogs and one policy. Not a status page. |
| [`client/README.md`](docs/client/README.md) | 🚩 **Where our own client stands, in one page** — readiness by subsystem, what is open ranked, and which of its twelve documents holds each line. It connects, walks, draws the world and its own windows and plays the shard's sound; it holds one session, nothing that breathes can be clicked, and nothing blends. Read it before opening any of them. |
| [`docs/client_versions.md`](docs/client_versions.md) | Which clients exist and which are played. |
| [`docs/development.md`](docs/development.md) | The environment: commands, toolchain, `target/`, MSRV. |
| [`render/README.md`](docs/render/README.md) | 🚩 **Where the lighting engine stands, in one page** — readiness by subsystem, what is left ranked, the defects a person can see, the normative spec for the pixel spaces, and which of the eleven lighting documents are still live. Read it before opening any of them. |
| [`render/evidence/pitfalls.md`](docs/render/evidence/pitfalls.md) | 🚩 **How a lit frame lies, and the order to ask it things in.** A bright line on a dark surface is not evidence of light. The four-rung ladder (`Kind` → the albedo control → `Flames` → `Normal`), the amplifier that makes a facing error read as a drawn artefact, and each pitfall with the wrong verdict it cost. Read it before diagnosing anything a person reported by looking. |
| [`render/design_model.md`](docs/render/design_model.md) | The model itself, as built: deferred shading, art as albedo, shadows by primitive identity. The three roots, what a fragment arrives with, and the census of what of this geometry is measured off the art rather than stood in for. No status and no queue — those are `render/README.md`. |
| [`render/evidence/2026-08-11-lighting-rebuild-phases.md`](docs/render/evidence/2026-08-11-lighting-rebuild-phases.md) | How phases 0–8 were built, what each one measured and what each one got wrong. The account, not the queue — read it for why a thing is the way it is. |
| [`render/evidence/2026-08-11-lighting-backlog-findings.md`](docs/render/evidence/2026-08-11-lighting-backlog-findings.md) | Every finding the rebuild turned up, with the number taken for it. Read it for a measurement; the ranked queue is `render/README.md`. |
| [`plans/render/lighting/PLAN.md`](plans/render/lighting/PLAN.md) | What is **not** built: phase 6i's last hole, phase 7's mobile normal, phase 8's sun, the content layer, the instruments, and the work parked on purpose. |
| **Consolidated into it** — [`lighting.md`](docs/archive/render/lighting.md), [`lighting_world.md`](docs/archive/render/lighting_world.md), [`lighting_raymarch.md`](docs/archive/render/lighting_raymarch.md), [`lighting_geometry.md`](docs/archive/render/lighting_geometry.md), [`lighting_height.md`](docs/archive/render/lighting_height.md), [`lighting_reference.md`](docs/render/reference/path_tracer.md), [`gbuffer.md`](docs/archive/render/gbuffer.md), [`world_coordinates.md`](docs/archive/render/world_coordinates.md) | The record of how each was built and why. Read one when you need its reasoning — not to find out what is left to do. |
| [`render/design_occluders.md`](docs/render/design_occluders.md) | The occlusion geometry, being rebuilt: one shape per surface, absolute world coordinates, a BVH, and no tile in the answer. The rebuild's phase 6e in full — its decisions are **made**, so read it before proposing another. |
| [`render/design_frame_assembly.md`](docs/render/design_frame_assembly.md) | 🚩 **One frame, however it was asked for.** A frame is assembled by hand in seven places, so parity between the client and every diagnostic tool is a coincidence rather than a property — read it before adding a caller or trusting a tool's picture. |
| [`render/design_footprints.md`](docs/render/design_footprints.md) | A static's box is the box the art drew. The other half of `design_occluders.md`, which put it out of scope by name: that plan changes how many primitives a surface is, this one changes what one primitive's *shape* is — the 31.6% of the world currently given a whole tile because "the art would not say". |
| [`render/design_pixel_spaces.md`](docs/render/design_pixel_spaces.md) | Six grids meet in this renderer and no document listed them. Not a glossary — a statement of **which pairs share a divisor**, because a sample landing exactly on a discontinuity is what `design_frame_assembly.md`'s window-parity defect was made of. |
| [`render/design_silhouettes.md`](docs/render/design_silhouettes.md) | The zigzags. A magnified frame draws its outlines at two resolutions — a box's edge per fragment, the art's alpha per texel — and they meet along one line. First attribute them with a debug view, then decide. |
| [`housing/README.md`](docs/housing/README.md) | 🚩 **Where a building stands, in one page** — a house, a house whose shape the shard invented, and a house that moves, in one domain because each is the previous one with a word changed. Readiness by part, what is enforced and by what, thirteen ranked open items, and which of its three design documents holds each line. Read it before opening any of them. |
| [`housing/design_house.md`](docs/housing/design_house.md) | **A house, and the ground it stands on.** The picture is free — every client already owns every house — so what the shard owes is the walls that stop you, the door that knows you, and the decay that takes it away. Eleven decisions, the five placement rules, and the staff exemption that is a row of a table rather than an early return, because a bypass that reopens a refusal another decision closed is a regression with a permission check on it. |
| [`housing/design_customisation.md`](docs/housing/design_customisation.md) | **A house whose shape nobody shipped.** The picture stops being free for exactly one kind of house, and the load-bearing decision is where a per-house component list lives — because `Terrain::multi_components` cannot hold one, for five reasons, and the fifth is why minting a multi id is not an escape hatch. |
| [`world/README.md`](docs/world/README.md) | 🚩 **Where the world stands, in one page** — the map, the search over it, and the five readers that each bake something off terrain. Readiness by subsystem, what is open ranked, and which document holds each line. The world is our own data now: a base set of chunks plus a log of patches, one revisioned snapshot every reader holds, an operator's edit reaching a connected screen, and every derived artefact carried over the chunks a publish named. Read it before opening any document below it. |
| [`world/design_layers.md`](docs/world/design_layers.md) | The one-page routing table over the three layers: **must a bake see it?** is the whole rule, every answer taken so far is a row, and the two rules that read as a contradiction — *"never an overlay"* against *"a house is a layer"* — are reconciled there. Read it before quoting either at a question about the other. |
| [`housing/design_boats.md`](docs/housing/design_boats.md) | **A house that moves.** Every hard decision follows from *moves*, not from *boat*. No parent transform — refused on the engine's own evidence, since mounting deletes the mount rather than carrying it. The hull stays out of `Obstructions`, which only ever subtracts and cannot say "there is somewhere to stand here". |
| [`combat/README.md`](docs/combat/README.md) | 🚩 **Where the fight stands, in one page** — the fight loop, the action object, the sight ray, the skill curve and Magery, in one domain because they are one dependency chain. Readiness by part, what is enforced and by what, twenty ranked open items, and which of its five design documents holds each line. Read it before opening any of them. |
| [`combat/design_actions.md`](docs/combat/design_actions.md) | 🚩 **A blow that takes time.** An action is an object on three separated axes — *kind* (what the impact does), *watch* (what releases it), *rules* (what the world does to it in between) — where every action releases on a clock and the watch only decides when that clock starts. Four verbs, twelve decisions, four `0xBF` subcommands carrying a beginning, an end, a refusal and a stage. Read it before changing anything about how a fight is timed. |
| [`combat/design_sight.md`](docs/combat/design_sight.md) | **The ray a shot is allowed by.** `sight_clear` is a *reading* of `sight::trace`, one loop and one eye height, because a diagnostic drawn from a second walk looks exactly like one drawn from the right one. Read its closing list before touching the rule — a shut door is opaque at every height, `EYE = 9` is everybody's eye, and nothing in the live layer but a door is in the way at all. |
| [`npc/README.md`](docs/npc/README.md) | 🚩 **Where the people and the creatures stand, in one page** — everything in the world that is not the player and not scenery: a brain, a townsperson, a pet, a guard, a party, a guild, a quest. Readiness by part, what is enforced and by what, sixteen ranked open items, and which of its five design documents holds each line. The one sentence to take from it: everything alive here decides on a beat and only ever returns a direction, and nothing here is told anything — progress, a crossing and a cancel are all *found* by a pass that diffs. |
| [`npc/design_brain.md`](docs/npc/design_brain.md) | **What a creature decides, and what it may keep.** The four phases, the two searches (a bounded exact one, then the baked coarse graph refined through live ground), the kept route whose lifetime is decided by *what* it is walking to, the remembered refusal, and the level of detail with the sector-crossing wake that has to come with it. Read it before touching anything that plans a step for a body. |
| [`world/design_interiors.md`](docs/world/design_interiors.md) | **A building, and what it lets you see.** Three asks, one seam — the storey as a person's choice rather than `UpdateMaxDrawZ`'s, a **sealed room as a black area**, and walls cut to the knee. The second needs an index of rooms, and the first is what makes it necessary: with the roof on, a sealed room is already invisible. R0 is a refactor with no feature in it, and every phase after it names which precondition it spends. |
| [`client/design_walk.md`](docs/client/design_walk.md) | 🚩 **One movement state, one clock, and what a refusal means.** Read it before touching anything that sends a step: the input rule (*an input joins the queue or rebuilds it; a step already begun ticks out*), the detour's four tiles, the two leeways, the lean measured on the screen, the dead zone and the turn ring, and the drain-then-resync cycle. Almost every walking defect this client has had was two timelines drifting apart, not the code that draws. |
| [`protocol/README.md`](docs/protocol/README.md) | 🚩 **Where the wire stands, in one page** — two sum types, one framing layer, and no bare integer left in either: every field carries a named type, client input carries a `Raw*` that only a named check promotes, and two tests enforce the allowlists. What is open ranked, and which of its eight documents holds each line. Read it before adding a packet or a field. |
| [`server/README.md`](docs/server/README.md) | 🚩 **Where the shard stands, in one page** — two loops and a channel between them, the connection as a row in the world and a phase in the binary, what a save takes and what a stop owes, and the tick-pace watchdog that can end a shard that has stopped keeping its own clock. What is open ranked, including the two unbounded waits. Read it before touching the loop, the login, or the store. |
| **Living plans** — [`outline.md`](docs/render/design_outline.md) | Multi-session refactors, each with a backlog of what's left undone — that backlog is where the next session starts. The connection and shutdown plans finished and became records under [`docs/server/`](docs/server/README.md). |
| [`CONTRIBUTING.md`](CONTRIBUTING.md) | What lands and how: branch, PR, review, merge commit, commit messages. |

## Working on this

```sh
cargo test --workspace          # includes doctests
cargo clippy --workspace --all-targets
cargo fmt --all
```

All three are expected to be silent — that's what CI runs on every PR.

## Non-goals

Reimplementing SphereScript. Parsing `.scp` at runtime. Source compatibility with
Sphere. Legacy save formats. Mimicking Sphere's internals.
