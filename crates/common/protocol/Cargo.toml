[package]
name = "openshard-protocol"
description = "Ultima Online wire protocol: client versioning, feature gates, packet encode/decode."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
# `0xD8` sends a house's design deflated, so the design packet needs zlib.
# `miniz_oxide` and not `flate2` because that crate's C backend is ruled out by
# this workspace's `unsafe_code = "deny"`; `uofiles` made the same choice for
# gumpart and the UOP multi reader.
miniz_oxide.workspace = true
serde.workspace = true

[build-dependencies]
serde_json.workspace = true
