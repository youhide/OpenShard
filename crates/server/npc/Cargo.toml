[package]
name = "openshard-npc"
description = "Townsfolk: bankers and vendors — their services, dialogue, and the little AI that greets, faces and wanders."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
openshard-ai.workspace = true
openshard-chat.workspace = true
openshard-combat.workspace = true
openshard-entities.workspace = true
openshard-gateway.workspace = true
openshard-items.workspace = true
# `Tile`, and `Doors` for the reading a creature plans its route under.
openshard-map.workspace = true
openshard-movement.workspace = true
openshard-protocol.workspace = true
openshard-state.workspace = true
tracing.workspace = true

# `build.rs` only: the default name lists are `data/names.json` and become
# `const`s before the crate compiles. Nothing here is linked into the shard.
[build-dependencies]
serde_json.workspace = true
