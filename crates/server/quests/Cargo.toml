[package]
name = "openshard-quests"
description = "Quests: offering, tracking, turning in, and the quest log gump — ServUO's Mondain's Legacy model, with the content left to the pack."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
openshard-ai.workspace = true
openshard-chat.workspace = true
openshard-combat.workspace = true
openshard-entities.workspace = true
openshard-gateway.workspace = true
openshard-items.workspace = true
# For `Doors`: which reading of the shut doors an escort plans under. The enum
# a step is asked with, rather than the `bool` it used to be — see
# `docs/world/research/terrain_seam.md`'s node E.
# `Doors`: which reading a quest's escort plans its route under.
openshard-map.workspace = true
openshard-movement.workspace = true
openshard-protocol.workspace = true
openshard-state.workspace = true
tracing.workspace = true
